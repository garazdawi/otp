%% %CopyrightBegin%
%%
%% SPDX-License-Identifier: Apache-2.0
%%
%% Copyright Ericsson AB 2021-2026. All Rights Reserved.
%%
%% %CopyrightEnd%

[1m  exit(Reason)[0m

  Raises an exception of class [4mexit[0m with exit reason [4mReason[0m.

  As evaluating this function causes an exception to be raised, it
  has no return value.

  The intent of the exception class [4mexit[0m is that the current
  process should be stopped (for example when a message telling a
  process to stop is received).

  This function differ from [4merror/1,2,3[0m by causing an exception of
  a different class and by having a reason that does not include the
  list of functions from the call stack.

  See the guide about errors and error handling for additional
  information.

  Example:

    1> exit(foobar).
    ** exception exit: foobar
    2> catch exit(foobar).
    {'EXIT',foobar}

  [4mNote[0m

    If a process calls [4mexit(kill)[0m and does not catch the
    exception, it will terminate with exit reason [4mkill[0m and also
    emit exit signals with exit reason [4mkill[0m (not [4mkilled[0m) to
    all linked processes. Such exit signals with exit reason [4mkill[0m
    can be trapped by the linked processes. Note that this means
    that signals with exit reason [4mkill[0m behave differently
    depending on how they are sent because the signal will be
    untrappable if a process sends such a signal to another
    process with [4merlang:exit_signal/2[0m.

[1m  exit(Dest, Reason)[0m

  Old form of [4mexit_signal/2[0m, with a quirk when sender and receiver
  are the same.

  [4mNote[0m

    The function [4merlang:exit/2[0m is named similarly to [4m[0m
    [4merlang:exit/1[0m but provides very different functionality. The [4m[0m
    [4merlang:exit/1[0m function should be used when the intent is to
    stop the current process by raising an exception of class [4m[0m
    [4mexit[0m.

    The [4merlang:exit_signal/2[0m function, or the old form [4m[0m
    [4merlang:exit/2[0m, should be used when the intent is to send an
    exit signal to another process. Note also that [4merlang:exit/1[0m
    raises an exception that can be caught, while [4m[0m
    [4merlang:exit_signal/2[0m does not cause any exception to be
    raised.

  [4mWarning[0m

    This function has a quirk: When a process [4mP[0m sends an exit
    signal with reason [4mnormal[0m to itself using this function,
    that is, [4merlang:exit(self(), normal)[0m, the behavior is as
    follows:

     • [4mP[0m exits with reason [4mnormal[0m if [4mP[0m is not trapping
       exits.

     • If [4mP[0m is trapping exits, the exit signal is transformed
       into a message [4m{'EXIT', From, normal}[0m, where [4mFrom[0m is [4m[0m
       [4mP[0m's process identifier, and delivered to [4mP[0m's message
       queue. Note that this differs from when a process sends
       an exit signal with reason [4mnormal[0m to another process
       than itself (see [4mexit_signal/2[0m for details). This
       behavior is kept for backward compatibility reasons. Use [4m[0m
       [4mexit_signal/2[0m for new code.

[1m  exit(Dest, Reason, OptList)[0m

[4mSince[0m:
  OTP 28.0

  Deprecated form of [4mexit_signal/3[0m, with a quirk when sender and
  receiver are the same.

  This allows passing options to [4mexit/2[0m, which preserves the
  behavior when a process [4mP[0m sends an exit signal with reason [4m[0m
  [4mnormal[0m to itself; see [4mexit/2[0m for details. For new code, use [4m[0m
  [4mexit_signal/3[0m.
